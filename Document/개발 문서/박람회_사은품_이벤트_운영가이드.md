# 박람회 사은품 이벤트 운영 가이드

| 항목 | 내용 |
|------|------|
| 문서 버전 | v1.0 |
| 작성일 | 2026-05-18 |
| 대상 이벤트 | 2026 국제소방안전박람회 사은품 증정 행사 |
| 적용 범위 | Frontend Only (백엔드/DB 변경 없음) |

---

## 1. 이벤트 개요

| 항목 | 내용 |
|------|------|
| 행사명 | 2026 국제소방안전박람회 |
| 일정 | 2026년 5월 20일(수) ~ 5월 22일(금) |
| 장소 | 대구 EXCO · BulC 부스 |
| 사은품 지급 대상 | 본 홈페이지에 회원가입한 사용자 **중 현재 언어 설정이 한국어(`ko`)인 사용자** |
| 식별 방식 | 로그인한 사용자가 사은품 페이지를 부스 직원에게 직접 제시 → 부스 직원이 직접 수령 완료 버튼 클릭 |

회원가입을 완료한 사용자에게 사은품을 증정하는 행사로, **백엔드/DB 변경 없이 프론트엔드만으로 동작**하도록 구현되었습니다. 정적 설정 파일 1개로 모든 동작이 제어됩니다.

**언어 제한 정책**: 본 이벤트는 대구에서 열리는 한국 행사이므로, 현재 언어 설정이 한국어(`ko`)인 사용자에게만 노출됩니다. 언어가 다른 경우(예: `en`) 배너 / 가입 후 리다이렉트 / 사은품 페이지 모두 자동으로 숨겨집니다.

언어 결정 우선순위 (기존 `LanguageContext` 로직 그대로 활용):
1. `localStorage.language` (사용자가 명시적으로 선택한 값)
2. `/api/language/detect` 응답 (Cloudflare `CF-IPCountry` 헤더 → 한국 IP면 `ko`, 그 외 `en`)
3. 폴백: `ko`

→ **한국에서 접속한 신규 사용자는 별도 설정 없이도 자동으로 `ko`** 가 적용되어 이벤트 대상에 자동 포함됩니다.

---

## 2. 동작 흐름

### 2.1 회원가입 → 사은품 페이지 자동 안내
1. 사용자가 회원가입 모달에서 가입 완료 → sessionStorage에 `pendingEventRedirect` 플래그 저장 (이벤트 활성 시)
2. 로그인 모달로 전환
3. 로그인 성공 시 LoginModal이 `useLanguage()` 훅으로 현재 언어 확인
4. **언어가 `ko`면** `/event/booth-gift` 페이지로 자동 이동, 아니면 정상 후속 동작 (`onSuccess`)
5. OAuth 회원가입(`/oauth/setup-password`)의 경우 가입+로그인 직후 동일하게 언어 확인 후 분기

### 2.2 마이페이지 상단 배너
- BoothGiftBanner가 `useLanguage()` 훅으로 현재 언어 직접 조회
- 다음 조건을 **모두** 충족할 때만 배너 노출:
  - 이벤트 활성 (`enabled` + 기간 내)
  - 현재 언어 == `ko`
- 배너 클릭 시 `/event/booth-gift` 로 이동

### 2.3 사은품 페이지 (`/event/booth-gift`)
- 로그인 필수 (비로그인 시 401 에러 페이지로 리다이렉트)
- 페이지 로드 시 `useLanguage()` 훅으로 언어 확인 → 한국어 사용자가 아니면 "이벤트가 진행되고 있지 않습니다" 화면 노출
- 표시 항목: 사용자 이름 / 이메일 / 발급(또는 수령) 시각 / 이벤트 기간 / 부스 위치
- 부스 직원이 페이지의 사용자 정보를 육안 확인 → 사은품 전달
- 전달 후 **부스 직원(운영자)이 직접 "사은품 수령 완료 처리" 버튼을 클릭** (운영자 전용 UI로 명시)

### 2.4 중복 수령 방지 (소프트 블록 — 운영자 액션)
- 사은품 페이지의 "수령 완료 처리" 버튼은 **부스 직원(운영자) 전용**으로 명시됩니다:
  - 노란 경고 박스로 "사용자가 누르지 마세요" 안내
  - 버튼은 짙은 색 배경 + 노란 점선 테두리 + `[부스 직원 전용]` 태그가 붙은 운영자 전용 스타일
  - 클릭 시 확인 다이얼로그: "부스 직원입니까? 사은품을 전달했습니까?"
- 클릭 확정 시 `localStorage`에 수령 기록 저장:
  - Key 형식: `boothGiftClaimed:<userId>:<eventStartAt>`
  - 값: `{ claimedAt, eventStartAt, eventEndAt }` (JSON)
- 동일 사용자가 같은 기기에서 페이지 재방문 시:
  - **"CLAIMED"** 녹색 뱃지 + ✓ 수령 완료 배너 + 수령 시각 표시
  - 수령 완료 버튼은 사라지고 "메인으로 돌아가기" 버튼만 노출 (취소 링크 없음)
- **이 차단은 정직한 사용자 대상 소프트 블록**입니다:
  - 같은 사용자가 다른 기기/브라우저/시크릿 모드로 접속하면 다시 페이지가 정상 노출됨
  - 사용자가 브라우저 캐시/스토리지를 지우면 다시 페이지가 정상 노출됨
  - 백엔드 추적이 없으므로 의도적 우회는 막을 수 없음
- **실수 클릭 복구 방법** (운영 도중 잘못 눌렀을 경우):
  - 브라우저 개발자 도구 → Application → Local Storage → `boothGiftClaimed:<userId>:...` 키 삭제 후 새로고침
  - 또는 사용자가 같은 계정으로 다른 기기/브라우저로 재접속
- 실수령 인원 정확 집계가 필요하면 부스에서 별도 수기 명단(이메일 끝 4자리 등) 병행 권장

---

## 3. 이벤트 On/Off 제어

**모든 제어는 단 하나의 파일로 수행됩니다:**

```
frontend/public/event-config.json
```

이 파일은 정적 파일로 서빙되므로 **수정 후 재배포만 하면 빌드 과정 없이 즉시 반영**됩니다 (브라우저 캐시는 최대 60초).

### 3.1 설정 항목

```json
{
  "boothGift": {
    "enabled": true,
    "startAt": "2026-05-20T00:00:00+09:00",
    "endAt": "2026-05-22T23:59:59+09:00",
    "title": "2026 국제소방안전박람회 사은품 이벤트",
    "subtitle": "회원가입을 완료해주셔서 감사합니다!",
    "description": "본 페이지를 부스에서 직원에게 보여주시면 사은품을 받으실 수 있습니다. 1인 1회에 한해 증정됩니다.",
    "boothInfo": "대구 EXCO · BulC 부스 (부스 위치 추후 안내)",
    "bannerTitle": "2026 국제소방안전박람회 사은품 이벤트 진행 중",
    "bannerCta": "사은품 페이지 보기",
    "bannerDescription": "대구 EXCO BulC 부스에서 페이지를 보여주시면 사은품을 증정합니다. (5/20 ~ 5/22)"
  }
}
```

| 필드 | 설명 | 비고 |
|------|------|------|
| `enabled` | 마스터 킬 스위치 | `false`면 기간과 무관하게 즉시 OFF |
| `startAt` | 이벤트 시작 시각 (ISO 8601, KST 권장) | 이 시각 이전엔 자동 비활성 |
| `endAt` | 이벤트 종료 시각 (ISO 8601, KST 권장) | 이 시각 이후엔 자동 비활성 |
| `title` | 사은품 페이지 메인 제목 | |
| `subtitle` | 사은품 페이지 부제 | |
| `description` | 사은품 페이지 본문 안내 | |
| `boothInfo` | 부스 위치 안내 | |
| `bannerTitle` | 마이페이지 배너 제목 | |
| `bannerCta` | 마이페이지 배너 버튼 텍스트 | |
| `bannerDescription` | 마이페이지 배너 설명 | |

### 3.2 활성화 조건

이벤트가 활성으로 판정되려면 **모든 조건이 충족**되어야 합니다:

1. `enabled === true`
2. 현재 시각이 `startAt ≤ now ≤ endAt` 범위 내
3. `startAt`, `endAt` 모두 유효한 ISO 8601 날짜 문자열

판정 로직: [`frontend/src/utils/eventConfig.ts`](../../frontend/src/utils/eventConfig.ts) 의 `isBoothGiftActive()`

---

## 4. 이벤트 OFF 시나리오별 절차

### 4.1 정상 종료 (이벤트 기간 만료 후)
**조치 불필요.** 시스템 시각이 `endAt`을 지나면 자동으로:
- 사은품 페이지: "이벤트가 진행되고 있지 않습니다" 표시 + 메인 이동 버튼
- 마이페이지 배너: 자동 숨김
- 회원가입 완료 시 일반 알림으로 변경 ("회원가입이 완료되었습니다. 로그인해주세요.")

### 4.2 긴급 중단 (이벤트 기간 중 즉시 OFF)
1. `frontend/public/event-config.json` 편집
2. `"enabled": true` → `"enabled": false` 로 변경
3. 변경된 파일을 배포 (Cloudflare Pages / nginx 정적 파일 서빙 환경 모두 동일)
4. 사용자 브라우저 측 캐시 TTL **최대 60초** 이후 모든 동작이 비활성화됨

> **참고:** `enabled: false`만으로도 즉시 OFF되므로, 날짜 필드는 그대로 유지해도 됩니다.

### 4.3 이벤트 일정 변경
1. `startAt` 또는 `endAt` 수정
2. 배포
3. **최대 60초 후** 새 날짜 기준으로 자동 활성/비활성 판정

### 4.4 이벤트 문구 변경
1. `title`, `subtitle`, `description`, `boothInfo`, `bannerTitle`, `bannerCta`, `bannerDescription` 등 자유롭게 수정
2. 배포
3. **최대 60초 후** 변경된 문구가 사용자에게 노출

### 4.5 행사 종료 후 코드 제거 (선택 사항)
이벤트가 완전히 끝난 뒤 추후 재사용 계획이 없다면 다음 파일들을 제거할 수 있습니다:

| 파일 | 비고 |
|------|------|
| `frontend/public/event-config.json` | 설정 파일 |
| `frontend/src/utils/eventConfig.ts` | 유틸리티 |
| `frontend/src/CategoryPages/Event/` | 사은품 페이지 디렉토리 전체 |
| `frontend/src/components/BoothGiftBanner.tsx` (+ CSS) | 배너 컴포넌트 |
| `frontend/src/index.tsx` | `/event/booth-gift` 라우트 제거 |
| `frontend/src/components/SignupModal.tsx` | `markPendingEventRedirect` 호출 부분 제거 |
| `frontend/src/components/LoginModal.tsx` | `consumePendingEventRedirect` 호출 부분 제거 |
| `frontend/src/pages/OAuthSetupPassword.tsx` | 이벤트 분기 제거 |
| `frontend/src/CategoryPages/MyPage/MyPage.tsx` | `<BoothGiftBanner />` 제거 |

> 단, 동일 인프라(`event-config.json`)로 향후 다른 박람회·행사를 진행할 수 있으므로 **유지하는 것을 권장**합니다.

---

## 5. 운영 시 주의사항

### 5.1 캐시 TTL 이해
- 브라우저 내부 캐시: **60초**
- nginx/CDN 캐시: 환경 설정에 따라 다름 (Cloudflare Pages 기본 정책 적용)
- 즉시 반영이 필요한 경우 사용자에게 "60초 후 새로고침" 안내 또는 CDN purge 수동 수행

### 5.2 부스 운영 체크리스트
- [ ] 부스 직원이 페이지의 **사용자 이름**과 **이메일**을 반드시 확인할 것
- [ ] 발급 시각(`발급 시각` 항목)을 통해 가짜 스크린샷 방지
- [ ] **사은품 전달 후 반드시 부스 직원이 직접 "사은품 수령 완료 처리" 버튼을 누를 것** (소프트 블록 작동)
- [ ] 사용자에게 직접 버튼을 누르라고 안내하지 말 것 (반드시 직원이 직접 클릭)
- [ ] 페이지가 "CLAIMED" 상태(녹색 뱃지)면 이미 같은 기기에서 수령한 사용자 → 사은품 재지급 금지
- [ ] 사용자가 "다른 기기로 다시 받으려 한다"고 주장하는 경우 별도 수기 명단 대조 권장
- [ ] 사은품 페이지는 **로그인 + 한국어 설정 사용자에게만** 보이므로, 부스에서 가입을 도울 경우 회원가입 → 로그인까지 안내 (한국 IP라면 자동으로 `ko` 설정됨)
- [ ] 사용자가 "페이지가 안 떠요"라고 할 경우 → 우측 상단 언어 설정이 한국어(KO)인지 확인 (영어로 바꿔둔 경우 한국어로 전환 안내)

### 5.3 비회원 회원가입 후 자동 안내
- 이메일 회원가입: 회원가입 완료 → 로그인 안내 → 로그인 성공 시 자동 이동
- OAuth 회원가입(구글/카카오/네이버): 비밀번호 설정 완료 → 자동 이동
- 두 경로 모두 `sessionStorage` / 함수 분기로 처리되므로 별도 안내 불필요

### 5.4 백엔드/DB 영향 없음
본 기능은 **순수 프론트엔드 정적 자원**으로 구성되며 다음 항목과 무관:
- 백엔드 코드 / API
- 데이터베이스 스키마 / 데이터
- 인증/세션 메커니즘 (기존 JWT 그대로 사용)
- 환경 변수

---

## 6. 관련 파일 위치

| 분류 | 경로 |
|------|------|
| 설정 파일 | `frontend/public/event-config.json` |
| 유틸리티 | `frontend/src/utils/eventConfig.ts` |
| 사은품 페이지 | `frontend/src/CategoryPages/Event/BoothGiftPage.tsx` |
| 사은품 페이지 CSS | `frontend/src/CategoryPages/Event/BoothGiftPage.css` |
| 마이페이지 배너 | `frontend/src/components/BoothGiftBanner.tsx` |
| 마이페이지 배너 CSS | `frontend/src/components/BoothGiftBanner.css` |
| 라우트 등록 | `frontend/src/index.tsx` |
| 이메일 회원가입 연동 | `frontend/src/components/SignupModal.tsx` |
| 이메일 로그인 연동 | `frontend/src/components/LoginModal.tsx` |
| OAuth 회원가입 연동 | `frontend/src/pages/OAuthSetupPassword.tsx` |
| 마이페이지 배너 마운트 | `frontend/src/CategoryPages/MyPage/MyPage.tsx` |

---

## 7. 변경 이력

| 일자 | 버전 | 작성자 | 변경 내용 |
|------|------|--------|-----------|
| 2026-05-18 | v1.0 | - | 최초 작성 (2026 국제소방안전박람회 대응) |
| 2026-05-18 | v1.1 | - | localStorage 기반 중복 수령 소프트 블록 추가 (수령 완료/취소 버튼) |
| 2026-05-19 | v1.2 | - | 국가(`KR`) 기반 노출 제한 추가, 수령 버튼을 부스 직원(운영자) 전용으로 UI 강화, 수령 표시 취소 링크 제거 |
| 2026-05-19 | v1.3 | - | 게이트 기준을 국가(`country`) → 언어(`language=ko`)로 변경 — 기존 LanguageContext의 IP 기반 자동 감지 로직 활용, 추가 fetch 불필요, 신규 가입자도 별도 설정 없이 즉시 대상 포함 |
