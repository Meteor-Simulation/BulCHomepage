/* technical-doc i18n — English. Index-aligned with the ko original (140 items). */
window.DOC_I18N = window.DOC_I18N || {};
window.DOC_I18N.en = [
/*0*/ "FDS(GPU) solves the incompressible reacting-flow equations under the low Mach number approximation, sharing the same mathematical foundation as NIST FDS. The background pressure $\\bar{p}$ is held constant and the pressure perturbation $p'$ is obtained by a projection method.",
/*1*/ "Under the low Mach approximation the velocity divergence is determined by thermal expansion:",
/*2*/ "Here $\\mathcal{D}$ is the <strong>divergence target</strong>, computed sequentially by three kernels (<code>compute_div_target</code>, <code>subtract_enthalpy_advection</code>, <code>add_d_source_to_div</code>).",
/*3*/ "Meaning of each term:",
/*4*/ "The thermal diffusivity is $\\alpha_{\\text{eff}} = k_{\\text{eff}} / (\\rho \\, c_p)$, where $k_{\\text{eff}} = k_{\\text{mol}}(T) + \\mu_t c_p / \\text{Pr}_t$.",
/*5*/ "FDS Fortran solves a transport equation for the sensible enthalpy $\\rho h_s$, whereas FDS(GPU) transports temperature directly. The source term is applied as $\\dot{q}'''/(\\rho c_p)$, and the temperature-dependent specific heat $c_p(T)$ and the Wilke mixing rule are updated every step.",
/*6*/ "Tracked species: <strong>Fuel</strong>, <strong>O$_2$</strong>, <strong>CO$_2$</strong>, <strong>CO</strong>, <strong>Soot</strong>, and <strong>H$_2$O</strong> (6 species). N$_2$ is computed as $Y_{\\text{N}_2} = 1 - \\sum Y_i$. H$_2$O was added to transport sprinkler evaporation and combustion products.",
/*7*/ "Effective diffusivity: $D_{\\text{eff}} = D_{\\text{mol}} + \\mu_t / (\\rho \\, \\text{Sc}_t)$, $\\text{Sc}_t = 0.5$.",
/*8*/ "Constants: $\\bar{p} = 101325$ Pa, $R = 8314.46$ J/(kmol&middot;K). Molecular weights $W_i$ (g/mol):",
/*9*/ "FDS(GPU) uses a <strong>Marker-and-Cell (MAC) staggered grid</strong>. This arrangement inherently prevents the checkerboard pressure instability that plagues collocated grids.",
/*10*/ "All arrays are stored row-major (C-order); $i$ varies fastest:",
/*11*/ "On a staggered grid the divergence is computed <strong>exactly</strong> from direct differences of the face velocities:",
/*12*/ "Because this discretization uses face values directly <strong>without interpolation</strong>, it is second-order accurate and avoids the Rhie-Chow interpolation required on collocated grids.",
/*13*/ "During velocity correction the pressure gradient is also a direct difference of adjacent cell-center values:",
/*14*/ "Face masks are generated from the cell-based <code>solid[i,j,k]</code> array:",
/*15*/ "The velocity at a solid face is forced to zero (no-penetration).",
/*16*/ "The pressure Poisson equation is solved directly to <strong>machine precision</strong> using cuFFT. No iterative method such as SOR/Multigrid is needed, so there are no convergence problems.",
/*17*/ "In the projection method the divergence of the predicted velocity $\\mathbf{u}^*$ is corrected toward the target $\\mathcal{D}$:",
/*18*/ "Discretization:",
/*19*/ "where $f_{i,j,k} = (\\text{div}(\\mathbf{u}^*) - \\mathcal{D}) / \\Delta t$.",
/*20*/ "cuFFT does not directly support the DCT/DST, so a standard R2C FFT is used after symmetry extension.",
/*21*/ "Extended array size: $(2N_i) \\times (2N_j) \\times (2N_k)$. For example, a 220&times;80&times;25 grid is extended to 440&times;160&times;50 = 3,520,000.",
/*22*/ "Divide by the eigenvalues of the Laplacian operator in the frequency domain:",
/*23*/ "Eigenvalues:",
/*24*/ "where $N_{ex} = 2N_i$, $N_{ey} = 2N_j$, $N_{ez} = 2N_k$. At $\\lambda = 0$ (the zero mode) we set $\\hat{p} = 0$ to fix the mean pressure.",
/*25*/ "cuFFT does not normalize the inverse transform, so we divide during extraction:",
/*26*/ "<strong>Symmetry Extend</strong> &mdash; $N_i \\times N_j \\times N_k \\to 2N_i \\times 2N_j \\times 2N_k$ (CUDA kernel <code>fft_symmetry_extend</code>)",
/*27*/ "<strong>Forward R2C FFT</strong> &mdash; <code>cufftExecR2C</code> (cuFFT library call)",
/*28*/ "<strong>Eigenvalue Division</strong> &mdash; $\\hat{f}/\\lambda$ in the frequency domain (CUDA kernel <code>fft_eigenvalue_divide</code>)",
/*29*/ "<strong>Inverse C2R FFT</strong> &mdash; <code>cufftExecC2R</code> (cuFFT library call)",
/*30*/ "<strong>Extract &amp; Normalize</strong> &mdash; extract the original size from the extended array, $/(N_{ex} N_{ey} N_{ez})$ (CUDA kernel <code>fft_extract_solution</code>)",
/*31*/ "<strong>Note:</strong> The cuFFT R2C output is Hermitian-symmetric, so the last dimension has $N_{ez}/2 + 1$ complex numbers. Total memory: $(N_{ex}/2 + 1) \\times N_{ey} \\times N_{ez} \\times 8$ bytes.",
/*32*/ "A TVD (Total Variation Diminishing) slope limiter that suppresses numerical oscillations while keeping second-order accuracy:",
/*33*/ "If the left/right slopes in a cell have different signs (an extremum) the slope is set to 0; otherwise the one with the smaller magnitude is chosen.",
/*34*/ "Self-advection at the u-face $(i, j, k)$:",
/*35*/ "where $v_c, w_c$ are tangential velocities interpolated to the face location. Each gradient is limited by MINMOD:",
/*36*/ "At open-boundary faces there are no ghost cells, so MINMOD returns 0 (the root cause of velocity divergence). As a fix a <strong>first-order upwind scheme</strong> is applied:",
/*37*/ "This treatment applies <strong>only to the normal-direction advective gradient</strong>. Tangential gradients and the diffusion Laplacian still use the existing clamped ghosts.",
/*38*/ "A cell-center scalar $\\phi$ (temperature or species mass fraction) is advected by the staggered face velocities:",
/*39*/ "where $u_c = \\frac{1}{2}(u_{i} + u_{i+1})$ is the cell-center average of the face velocities.",
/*40*/ "FDS(GPU) supports 4 subgrid-scale (SGS) turbulence models. The default is the <strong>Deardorff TKE</strong> model, the same as FDS.",
/*41*/ "$k_{\\text{sgs}}$ is the subgrid turbulent kinetic energy, updated each time step through production, dissipation, and diffusion terms.",
/*42*/ "Wall-Adapting Local Eddy-viscosity. Captures the near-wall $y^3$ scaling without a damping function.",
/*43*/ "Based on invariants of the velocity gradient tensor. Automatically $\\mu_t \\to 0$ in laminar flow.",
/*44*/ "On the staggered grid the diagonal components are computed directly:",
/*45*/ "Off-diagonal components are averaged from faces to the cell center:",
/*46*/ "$\\text{Pr}_t = 0.5$, $\\text{Sc}_t = 0.5$ (FDS defaults).",
/*47*/ "Implements FDS's EDC (Eddy Dissipation Concept) mixing-controlled combustion model. The chemical reaction rate is limited by the mixing rate of the reactants.",
/*48*/ "where $u_{\\text{sgs}} = \\mu_t / (\\rho \\, C_s \\, \\Delta)$. Lower bound: $\\tau_{\\text{mix}} \\ge \\Delta t$.",
/*49*/ "Whichever of fuel and oxygen is deficient determines the reaction rate:",
/*50*/ "where $s$ = stoichiometric O$_2$/fuel ratio (default 1.2).",
/*51*/ "Oxygen-limit (LOI) based extinction, identical to FDS's EXTINCTION_1 mode:",
/*52*/ "where $y_{\\text{CO}}, y_{\\text{soot}}$ are the CO_YIELD and SOOT_YIELD inputs.",
/*53*/ "Fuel and heat are injected at the fire surface:",
/*54*/ "where $f(t)$ is the time-scaling factor from the RAMP and $A_{\\text{cell}}$ is the intersection area of the fire and the cell.",
/*55*/ "FDS(GPU) uses a 6-direction finite volume method (FVM6) radiation model. At each cell it computes the radiative energy exchange with the 6 orthogonal neighbor cells, including wall emission.",
/*56*/ "CO$_2$/H$_2$O absorption decreases at high temperature due to vibrational-rotational saturation, while soot increases proportionally to temperature in the Rayleigh regime.",
/*57*/ "where $N_{\\text{nb}}$ is the number of non-solid neighbors (max 6), $\\alpha_s$ is the reabsorption scaling factor, and $\\chi_r$ is the radiative fraction (default 0.35). For wall-adjacent cells, wall emission ($\\varepsilon_w \\sigma T_w^4$, $\\varepsilon_w = 0.9$) is added to the absorption term.",
/*58*/ "<strong>Note:</strong> FVM6 is a GPU-optimized approximation of FDS's 104-direction FVM RTE solver. Using only 6 orthogonal directions makes it fully parallelizable with no inter-cell dependencies.",
/*59*/ "where $\\theta = T / 1000$ [K]. Valid range: 200 &ndash; 2500 K.",
/*60*/ "Inflow/Outflow determination: decided by the sign of the velocity at the boundary normal face.",
/*61*/ "Wall heat transfer: the <code>apply_wall_heat_transfer</code> kernel computes convective heat transfer, while solid conduction is handled by <code>solid_heat_conduction_CN</code> (1D Crank-Nicolson, 10-node depth profile) plus <code>solid_heat_conduction_3d</code> (3D complement).",
/*62*/ "At faces where the face mask (<code>solid_u, solid_v, solid_w</code>) is 1, the velocity is forced to zero. Cell-center scalars are not updated in solid cells.",
/*63*/ "This guarantees that the actual Courant number $C = \\Delta t \\cdot \\max(\\cdots) \\le 0.4$ always holds.",
/*64*/ "Uses 2-stage predictor-corrector time integration (2nd-order in time). A predictor-only mode can be switched on via an environment variable:",
/*65*/ "<strong>CFL &rarr; Adaptive $\\Delta t$</strong><br><code>compute_cfl_staggered</code> &rarr; max velocity reduction &rarr; $\\Delta t$ calculation",
/*66*/ "<strong>Save $\\mathbf{u}^n, T^n, \\rho^n, Y_i^n$</strong><br><code>copy_field</code>: $u \\to u_n, \\; v \\to v_n, \\; w \\to w_n, \\; T \\to T_n, \\; \\rho \\to \\rho_n, \\; Y_i \\to Y_{i,n}$",
/*67*/ "<strong>Material Properties</strong><br><code>compute_material_properties</code>: $\\mu(T), c_p(T), k(T) \\to \\mu_{\\text{eff}}, k_{\\text{eff}}, c_p$",
/*68*/ "<strong>Turbulence</strong><br><code>compute_turbulent_viscosity_deardorff</code> (or <code>_wale</code>, <code>_vreman</code>, <code>_smagorinsky</code>): $\\mathbf{u}^n, \\rho \\to \\mu_t$",
/*69*/ "<strong>Stress &amp; Wall Model</strong><br>\n    <code>compute_stress_divergence</code>: $\\tau_u, \\tau_v, \\tau_w$<br>\n    <code>compute_baroclinic</code>: baroclinic torque<br>\n    <code>apply_wall_model</code>, <code>apply_wall_heat_transfer</code><br>\n    <code>solid_heat_conduction_CN</code>, <code>solid_heat_conduction_3d</code>\n  ",
/*70*/ "<strong>Pyrolysis</strong><br>\n    <code>compute_pyrolysis</code>: Arrhenius pyrolysis<br>\n    <code>compute_combustible_boundary</code>: combustible boundary surface\n  ",
/*71*/ "<strong>Source Terms</strong><br>\n    Zero all sources<br>\n    <code>apply_fire_source</code>: fuel injection + surface heating<br>\n    <code>compute_combustion_v2</code>: $Y_f, Y_{O_2}, T, \\rho, \\mu_t \\to \\dot{q}, S_i$ (LOI extinction)<br>\n    <code>compute_radiation_fvm6</code>: $T, Y, \\kappa(T) \\to \\dot{q}_{\\text{rad}}$ (6-direction FVM)\n  ",
/*72*/ "<strong>Divergence Target</strong><br>$\\mathcal{D}$ via <code>compute_div_target</code> + <code>subtract_enthalpy_advection</code> + <code>add_d_source_to_div</code>",
/*73*/ "<strong>Velocity Advection</strong><br><code>advect_velocity_staggered</code>: $\\mathbf{u}^n \\to \\mathbf{u}^*$ (advection + diffusion + buoyancy + stress + baroclinic)",
/*74*/ "<strong>Divergence of $\\mathbf{u}^*$</strong><br><code>compute_divergence_staggered</code>",
/*75*/ "<strong>Poisson RHS</strong><br>$f = (\\nabla \\cdot \\mathbf{u}^* - \\mathcal{D}) / \\Delta t$ (CUDA kernel)",
/*76*/ "<strong>FFT Pressure Solve</strong><br>\n    <code>fft_symmetry_extend</code> &rarr; <code>cufftExecR2C</code> &rarr; <code>fft_eigenvalue_divide</code> &rarr; <code>cufftExecC2R</code> &rarr; <code>fft_extract_solution</code>\n  ",
/*77*/ "<strong>Velocity Correction</strong><br><code>correct_velocity_staggered</code>: $\\mathbf{u}^{n+1} = \\mathbf{u}^* - \\Delta t \\, \\nabla p / \\rho$<br><code>apply_vent_velocity</code>: apply vent velocity profile",
/*78*/ "<strong>Scalar Transport</strong><br>\n    <code>compute_diffusivities</code>: $\\alpha_{\\text{eff}}, D_{\\text{eff}}$<br>\n    <code>transport_scalar_staggered</code> &times; 7: $T, Y_f, Y_{O_2}, Y_{CO_2}, Y_{CO}, Y_{\\text{soot}}, Y_{\\text{H}_2\\text{O}}$<br>\n    <code>normalize_species</code>: normalize $\\sum Y_i = 1$\n  ",
/*79*/ "<strong>Density Update</strong><br><code>update_density_v2</code>: $\\rho = \\bar{p} W_{\\text{mix}} / (R T)$<br><code>apply_eos_consistency</code>: equation-of-state consistency correction",
/*80*/ "<strong>Boundary Conditions</strong><br><code>apply_bc_staggered</code>: Open/Wall BC enforcement",
/*81*/ "<strong>Predictor-Corrector Averaging</strong><br><code>average_fields</code>: $\\phi^{n+1} = (\\phi^n + \\phi^{**})/2$ (after corrector)<br><code>average_rhoY</code>: density-species averaging (for P-C)",
/*82*/ "<strong>Lagrangian Particles</strong><br>\n    <code>particle_find_free_slots</code>: GPU slot recycling<br>\n    <code>particle_scatter_inject</code>: sprinkler particle injection<br>\n    <code>particle_update</code>: particle transport (drag, evaporation, gravity)<br>\n    <code>particle_apply_momentum_sources</code>: particle-gas momentum coupling\n  ",
/*83*/ "<strong>Diagnostics &amp; Output</strong><br><code>compute_visibility</code>, <code>interpolate_velocity_to_center</code>, <code>sample_device_point</code>, <code>compute_bndf</code>, <code>clamp_velocity</code>, <code>compute_velocity_error</code>, <code>compute_hrr_sum</code>, <code>compute_total_mass</code>, SLCF/S3D/PL3D/BNDF output",
/*84*/ "Total kernel calls: ~148 kernel launches + 2 cuFFT calls per timestep (2 iterations when P-C is active).",
/*85*/ "88 CUDA kernels are implemented in <code>kernels.cu</code>. All are declared <code>extern \"C\" __global__</code>, compiled to PTX, and loaded at runtime via the Driver API.",
/*86*/ "Initializes an array to a constant value. $\\text{field}[i] = \\text{value}$ for $i \\in [0, n)$.",
/*87*/ "Array copy. Used to save $\\mathbf{u}^n$ at the start of the predictor step.",
/*88*/ "Generates face masks from the cell-center solid mask. $\\text{solid\\_u}[i] = \\text{solid}[i-1] \\lor \\text{solid}[i]$.",
/*89*/ "Average of two fields: $\\text{result}[i] = 0.5 (a[i] + b[i])$. Used in the predictor-corrector.",
/*90*/ "Sutherland viscosity + polynomial $c_p$ + Prandtl conductivity. $\\mu_{\\text{eff}} = \\mu_{\\text{mol}}(T) + \\mu_t$.",
/*91*/ "Deardorff TKE (default), WALE, Vreman, Smagorinsky &mdash; 4 selectable models. The 6 strain-rate components are computed on the staggered grid.",
/*92*/ "3-timescale mixing combustion. LOI-based extinction, species source terms, and heat release.",
/*93*/ "FVM6 6-direction radiation: temperature-dependent Planck mean absorption coefficient + neighbor-cell radiative exchange + wall emission. $\\kappa(T, Y_{\\text{CO}_2}, Y_{\\text{soot}}, Y_{\\text{H}_2\\text{O}})$.",
/*94*/ "Fuel injection at fire cells + TMP_FRONT surface heating. Applies the RAMP factor.",
/*95*/ "$\\nabla \\cdot \\mathbf{u} = (u_{i+1}-u_i)/\\Delta x + (v_{j+1}-v_j)/\\Delta y + (w_{k+1}-w_k)/\\Delta z$.",
/*96*/ "Three kernels in sequence: <code>compute_div_target</code> $(\\dot{q} - \\dot{q}_{\\text{rad}} + \\nabla\\cdot k\\nabla T)/(\\rho c_p T)$ + <code>subtract_enthalpy_advection</code> $-\\mathbf{u}\\cdot\\nabla(\\rho h_s)/(\\rho c_p T)$ + <code>add_d_source_to_div</code> species-source contribution.",
/*97*/ "Symmetrically extends the RHS to $(2N_i) \\times (2N_j) \\times (2N_k)$. Odd/even symmetry depending on the BC type.",
/*98*/ "$\\hat{f}/\\lambda$ division in the frequency domain. The $\\lambda = 0$ mode is set to $\\hat{p} = 0$.",
/*99*/ "Extracts $p[i,j,k] = p_{\\text{ext}}[i,j,k] / (N_{ex} N_{ey} N_{ez})$ from the extended array.",
/*100*/ "$u^{n+1} = u^* - \\Delta t \\cdot \\nabla p$. Factor-2 Dirichlet correction at open BC. Solid face: $u=0$.",
/*101*/ "MINMOD TVD advection + Laplacian diffusion + buoyancy. First-order upwind at open boundaries. Includes all faces (u: 0..ni, v: 0..nj, w: 0..nk).",
/*102*/ "Cell-center scalar advection (MINMOD TVD) + diffusion + source. If <code>cp_arr != NULL</code> then $S/(\\rho c_p)$ (temperature), else $S/\\rho$ (species). Clamping: $T \\in [200, 2500]$, $Y \\in [0, 1]$.",
/*103*/ "$W_{\\text{mix}} = 1/\\sum(Y_i/W_i)$, $\\rho = \\bar{p} W/(RT)$, clamped to $[0.1, 5.0]$ kg/m&sup3;.",
/*104*/ "$\\alpha = k/(\\rho c_p) + \\mu_t/(\\rho \\, \\text{Pr}_t)$, $D = D_0 + \\mu_t/(\\rho \\, \\text{Sc}_t)$.",
/*105*/ "Block-level shared-memory reduction to compute $\\max(|u|/\\Delta x + |v|/\\Delta y + |w|/\\Delta z)$. Global maximum via <code>atomicMax</code>.",
/*106*/ "$\\text{vis} = C / (K_m \\cdot \\rho \\cdot Y_{\\text{soot}})$, $K_m = 8700$ m&sup2;/kg, $C = 3$.",
/*107*/ "Face &rarr; cell-center interpolation: $u_c = (u_i + u_{i+1})/2$. For output and diagnostics.",
/*108*/ "Extracts a 2D slice at $z = k_{\\text{level}}$ from the 3D field.",
/*109*/ "Applies Open/Wall BC at boundary faces. Ambient values on inflow, interior copy on outflow.",
/*110*/ "Face-centered viscous stress tensor divergence (VTRM). Computes $\\tau_u, \\tau_v, \\tau_w$.",
/*111*/ "Baroclinic torque. Cross product of the density gradient and pressure gradient.",
/*112*/ "Wall shear stress. Wall-function-based tangential velocity correction.",
/*113*/ "Wall convective heat transfer. Nusselt-number-based heat flux.",
/*114*/ "1D Crank-Nicolson solid heat conduction (N_DEPTH=10). Temperature profile through the wall thickness.",
/*115*/ "3D solid heat conduction complement. Heat exchange between adjacent solid cells.",
/*116*/ "Arrhenius pyrolysis. Mass release rate as a function of solid surface temperature.",
/*117*/ "Combustible boundary surface. Fuel generation at the surface of a combustible solid.",
/*118*/ "Lagrangian particle transport. Drag, evaporation, and gravity combined. SoA layout.",
/*119*/ "GPU slot recycling. Finds inactive particle slots for reuse.",
/*120*/ "Sprinkler particle injection. Droplet generation at the nozzle location.",
/*121*/ "Particle-gas momentum coupling. Applied to the gas source terms via <code>atomicAdd</code>.",
/*122*/ "Species mass-fraction normalization. Ensures $\\sum Y_i = 1$.",
/*123*/ "Applies the vent velocity profile. Enforces velocity on VENT-specified faces.",
/*124*/ "Total HRR. Sums the heat release rate over the whole domain.",
/*125*/ "Total mass conservation check. Computes $\\sum \\rho \\cdot \\Delta V$.",
/*126*/ "Vector AXPY operation. $y = \\alpha x + y$.",
/*127*/ "Vector scaling. $x = \\alpha \\cdot x$.",
/*128*/ "HRR sum reduction. Block-level shared-memory reduction.",
/*129*/ "Density-species averaging (for P-C). $(\\rho Y)^{n+1} = ((\\rho Y)^n + (\\rho Y)^{**})/2$.",
/*130*/ "Equation-of-state consistency correction. Ensures mutual consistency of $\\rho, T, Y_i$.",
/*131*/ "Sensor point sampling. Extracts field values at DEVC locations.",
/*132*/ "Wall heat flux output (BNDF). Generates Smokeview BNDF-format data.",
/*133*/ "Velocity upper-bound limiting. Velocity clamping for numerical stability.",
/*134*/ "Velocity error diagnostics. Monitors divergence error and maximum velocity.",
/*135*/ "Instead of static linking, <strong>runtime dynamic loading</strong> is used so the build has no CUDA dependency:",
/*136*/ "CUDA kernels are compiled to PTX (Parallel Thread Execution) intermediate code and JIT-compiled at runtime:",
/*137*/ "Most kernels process the whole array with 1D indexing. $(i, j, k)$ is recovered from the index by modular arithmetic.",
/*138*/ "All GPU buffers are allocated with <code>cuMemAlloc</code> and freed automatically by Rust's <code>Drop</code> trait."
];
