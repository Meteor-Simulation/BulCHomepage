# 배포 환경 변수 설정 (예시)
# 이 파일을 복사하여 .env.prod 파일로 저장하고 실제 값을 입력하세요
# 중요: 이 파일은 절대 Git에 커밋하지 마세요!

# 데이터베이스 설정 (보안 강화된 비밀번호 사용)
DB_NAME=bulc_homepage_db
DB_USER=bulc_user
DB_PASSWORD=CHANGE_THIS_TO_SECURE_PASSWORD

# 프론트엔드 설정
FRONTEND_PORT=80
