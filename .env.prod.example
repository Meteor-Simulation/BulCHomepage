# ============================================================================
# BulC Homepage - 프로덕션 환경 변수 (예시)
# ============================================================================
# 이 파일을 .env.prod로 복사하여 실제 값을 입력하세요.
# 명령어: cp .env.prod.example .env.prod
# 중요: 이 파일은 절대 Git에 커밋하지 마세요!
# ============================================================================

# ============================================================================
# 1. 공통 설정
# ============================================================================

# --- Database ---
DB_HOST=your-rds-endpoint.amazonaws.com
DB_PORT=5432
DB_NAME=bulc_homepage_db
DB_USER=bulc_prod_user
DB_PASSWORD=CHANGE_TO_STRONG_PASSWORD

# --- Server ---
BACKEND_PORT=8080
FRONTEND_PORT=80
NGINX_PORT=443
SERVER_URL=https://your-domain.com

# --- JWT ---
# 주의: 반드시 강력한 랜덤 시크릿으로 변경하세요!
# 생성 방법: openssl rand -base64 64
JWT_SECRET=CHANGE_TO_SECURE_64_CHAR_SECRET
JWT_ACCESS_EXPIRATION=600000
JWT_REFRESH_EXPIRATION=604800000

# ============================================================================
# 2. Web Module
# ============================================================================

# --- OAuth2 ---
NAVER_CLIENT_ID=your_prod_naver_client_id
NAVER_CLIENT_SECRET=your_prod_naver_client_secret
OAUTH2_REDIRECT_URI=https://your-domain.com/oauth/callback

# --- Microsoft Graph API ---
MS_TENANT_ID=your_prod_tenant_id
MS_CLIENT_ID=your_prod_client_id
MS_CLIENT_SECRET=your_prod_client_secret

MAIL_FROM=no-reply@your-domain.com
MAIL_FROM_ACCOUNTS=accounts@your-domain.com
MAIL_FROM_BILLING=billing@your-domain.com
MAIL_REPLY_TO=support@your-domain.com

# ============================================================================
# 3. Licensing Module
# ============================================================================

# 방법1 (권장): Base64 인코딩된 PEM 문자열 (K8s Secret, AWS SSM 등)
# 생성: base64 -w0 secrets/session_token_private_key.pem
# LIC_PRIVATE_KEY_BASE64=<base64-encoded-pem>

# 방법2: Docker/K8s volume mount
LIC_PRIVATE_KEY_PATH=/run/secrets/session_token_private_key.pem
LIC_PUBLIC_KEY_PATH=/run/secrets/session_token_public_key.pem

LIC_TOKEN_TTL_MINUTES=15
LIC_TOKEN_ISSUER=bulc-license-server
LIC_STALE_THRESHOLD_MINUTES=30

# ============================================================================
# 4. 추가 보안 설정 (필요시)
# ============================================================================
# CORS_ALLOWED_ORIGINS=https://yourdomain.com
# ALLOWED_HOSTS=yourdomain.com,www.yourdomain.com
